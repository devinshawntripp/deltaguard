---
phase: 06-cluster-deployment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - rust_scanner/Cargo.toml
  - rust_scanner/Cargo.lock
  - rust_scanner/CHANGELOG.md
  - rust_scanner/.github/workflows/release.yml
autonomous: true
requirements: [INFR-02]
must_haves:
  truths:
    - "Scanner v1.10.2 GitHub release exists with 4 platform binaries (linux-amd64, linux-arm64, darwin-amd64, darwin-arm64)"
    - "Cargo.toml version field reads 1.10.2"
    - "CHANGELOG.md has a [1.10.2] entry documenting Phase 5.1 vulndb SQLite features"
  artifacts:
    - path: "rust_scanner/Cargo.toml"
      provides: "Version bump to 1.10.2"
      contains: 'version = "1.10.2"'
    - path: "rust_scanner/CHANGELOG.md"
      provides: "Release notes for 1.10.2"
      contains: "[1.10.2]"
    - path: "rust_scanner/Cargo.lock"
      provides: "Regenerated lockfile matching Cargo.toml"
  key_links:
    - from: "git tag v1.10.2"
      to: ".github/workflows/release.yml"
      via: "tag push triggers CI"
      pattern: 'on:\n  push:\n    tags:\n      - "v\*"'
---

<objective>
Create the scanner v1.10.2 GitHub release so the worker pods can download the new binary at startup.

Purpose: The worker entrypoint.sh downloads the scanner binary from GitHub releases via `curl -fsSL https://scanrook.sh/install`. The scanner release MUST exist before worker pods are restarted. This plan creates that release.

Output: GitHub release `v1.10.2` at `devinshawntripp/rust_scanner` with 4 platform binaries attached.
</objective>

<execution_context>
@/Users/devintripp/.claude/get-shit-done/workflows/execute-plan.md
@/Users/devintripp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-cluster-deployment/06-CONTEXT.md
@.planning/phases/06-cluster-deployment/06-RESEARCH.md

@rust_scanner/Cargo.toml
@rust_scanner/CHANGELOG.md
@rust_scanner/.github/workflows/release.yml

<interfaces>
<!-- Scanner release mechanics -->
From rust_scanner/Cargo.toml:
- Current version: "1.10.1" (line 3)
- Must bump to: "1.10.2"

From rust_scanner/.github/workflows/release.yml:
- Triggered by: push tags matching "v*"
- Builds 4 platform binaries via matrix
- Uses `cargo build --release --locked` (Cargo.lock MUST be fresh)
- Release body is hardcoded at lines 93-111 — must update for v1.10.2 features

From rust_scanner/CHANGELOG.md:
- Latest entry: [1.10.0] dated 2026-03-04
- No [1.10.1] entry exists (version was bumped without changelog — known tech debt)
- Must add [1.10.2] entry above [1.10.0]

Version history:
- v1.10.0: RHEL dedup + DMG scanning (Phase 4)
- v1.10.1: Tagged without CHANGELOG (Phase 4 bump)
- v1.10.2: Phase 5.1 vulndb SQLite features (this release)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump scanner to v1.10.2 with CHANGELOG and release notes</name>
  <files>rust_scanner/Cargo.toml, rust_scanner/Cargo.lock, rust_scanner/CHANGELOG.md, rust_scanner/.github/workflows/release.yml</files>
  <action>
In the `rust_scanner/` repo directory:

1. **Bump Cargo.toml version** (line 3): Change `version = "1.10.1"` to `version = "1.10.2"`.

2. **Add CHANGELOG.md entry** — Insert a `## [1.10.2] - 2026-03-04` section above the existing `## [1.10.0]` entry:

```markdown
## [1.10.2] - 2026-03-04

### Added
- SQLite vulndb query integration for OSV batch queries (local cache lookup before remote API)
- SQLite vulndb query integration for EPSS enrichment (risk scoring from local cache)
- SQLite vulndb query integration for KEV enrichment (CISA Known Exploited Vulnerabilities from local cache)
- Magic-byte detection for gzip vs raw SQLite in vulndb fetch
- Dict-aware decompression for zstd-compressed vulndb payloads

### Changed
- OSV batch queries now check local SQLite vulndb cache first, falling back to remote OSV API
```

3. **Update release.yml body** (lines 93-111) — Replace the hardcoded release body with v1.10.2 content:

```yaml
          body: |
            ## ScanRook v${{ env.VERSION }}

            ### New Features
            - **Local vulndb SQLite cache** — OSV batch queries now check a local SQLite database before calling the remote API, dramatically reducing API calls
            - **EPSS risk scoring** — Findings enriched with Exploit Prediction Scoring System data from local cache
            - **KEV enrichment** — CISA Known Exploited Vulnerabilities flagged from local cache
            - **Smart decompression** — Magic-byte detection for gzip vs raw SQLite; dict-aware zstd decompression

            ### Install
            ```bash
            curl -fsSL https://scanrook.sh/install | bash
            ```

            Or download the binary for your platform below.
```

4. **Regenerate Cargo.lock** — CRITICAL: CI uses `cargo build --locked` which fails on stale lockfile:
```bash
cd rust_scanner && cargo generate-lockfile
```

5. **Commit** on a branch, then merge to main:
```bash
git checkout -b chore/bump-v1.10.2
git add Cargo.toml Cargo.lock CHANGELOG.md .github/workflows/release.yml
git commit -m "chore: bump version to v1.10.2"
git checkout main && git merge chore/bump-v1.10.2
```

6. **Tag and push** (triggers GitHub Actions release.yml):
```bash
git tag v1.10.2
git push origin main --tags
```

7. **Wait for CI** — The release.yml builds 4 platform binaries (typically 8-15 minutes). Monitor with:
```bash
gh run list --repo devinshawntripp/rust_scanner --limit 3
```
Poll until the run completes. Then verify the release exists:
```bash
gh release view v1.10.2 --repo devinshawntripp/rust_scanner
```
Confirm all 4 tar.gz assets are listed (linux-amd64, linux-arm64, darwin-amd64, darwin-arm64).
  </action>
  <verify>
    <automated>gh release view v1.10.2 --repo devinshawntripp/rust_scanner --json assets --jq '.assets | length'</automated>
  </verify>
  <done>GitHub release v1.10.2 exists at devinshawntripp/rust_scanner with 4+ assets (4 platform tarballs + checksums). Cargo.toml reads version 1.10.2. CHANGELOG.md has [1.10.2] entry.</done>
</task>

</tasks>

<verification>
- `gh release view v1.10.2 --repo devinshawntripp/rust_scanner` shows the release with all platform binaries
- `grep 'version = "1.10.2"' rust_scanner/Cargo.toml` matches
- `grep '\[1.10.2\]' rust_scanner/CHANGELOG.md` matches
</verification>

<success_criteria>
Scanner v1.10.2 release is live on GitHub with all 4 platform binaries. The `scanrook-linux-amd64` asset specifically must be available — this is what the worker pods download via `https://scanrook.sh/install`.
</success_criteria>

<output>
After completion, create `.planning/phases/06-cluster-deployment/06-01-SUMMARY.md`
</output>

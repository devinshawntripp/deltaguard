---
phase: 05.2-ui-navigation-regressions
verified: 2026-03-04T09:00:00Z
status: passed
score: 6/6 must-haves verified
re_verification: false
---

# Phase 5.2: UI Navigation Regressions Verification Report

**Phase Goal:** Public site navigation works correctly for both authenticated and unauthenticated users without unexpected redirects
**Verified:** 2026-03-04T09:00:00Z
**Status:** PASSED
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| #  | Truth                                                                                          | Status     | Evidence                                                                                                      |
|----|-----------------------------------------------------------------------------------------------|------------|---------------------------------------------------------------------------------------------------------------|
| 1  | Authenticated user visiting / sees the marketing home page, not a redirect to /dashboard      | VERIFIED   | `redirect("/dashboard")` removed from `page.tsx` (commit f7b84b6); `isPublicPath("/")` returns true in proxy |
| 2  | Authenticated user sees 'Go to Dashboard' CTA instead of 'Sign in' on home page               | VERIFIED   | `page.tsx` line 31-35: ternary `isSignedIn ? <Link href="/dashboard">Go to Dashboard</Link> : Sign in`        |
| 3  | Unauthenticated user sees 'Sign in' CTA on home page (unchanged behavior)                     | VERIFIED   | Same ternary — `isSignedIn` false branch renders `<Link href="/signin">Sign in</Link>`                        |
| 4  | Home link from /docs navigates to / and shows the home page for all users                     | VERIFIED   | `PublicNavClient.tsx` line 13: `<Link href="/" className="app-nav-link">Home</Link>` — no auth gate           |
| 5  | Install link remains external to https://scanrook.sh                                          | VERIFIED   | `PublicNavClient.tsx` line 17: `<a href="https://scanrook.sh" className="app-nav-link">Install</a>`           |
| 6  | /signin still redirects authenticated users to /dashboard                                     | VERIFIED   | `src/app/(auth)/signin/page.tsx` line 12-14: redirect block intact, untouched by this phase                   |

**Score:** 6/6 truths verified

### Required Artifacts

| Artifact                         | Expected                                          | Status     | Details                                                                                   |
|----------------------------------|---------------------------------------------------|------------|-------------------------------------------------------------------------------------------|
| `src/app/page.tsx`               | Home page without auth redirect, conditional CTA  | VERIFIED   | 76 lines; `redirect` import gone; `isSignedIn` computed; ternary CTA present; substantive |
| `src/components/PublicNavClient.tsx` | Home link to `/`, Install link external        | VERIFIED   | `<Link href="/">Home</Link>` confirmed; `<a href="https://scanrook.sh">Install</a>` confirmed |
| `src/app/docs/layout.tsx`        | No auth redirect logic, wraps PublicSiteShell     | VERIFIED   | Wraps `<PublicSiteShell>` only; no session check; no redirect import                      |

### Key Link Verification

| From                      | To                    | Via                                         | Status   | Details                                                                                       |
|---------------------------|-----------------------|---------------------------------------------|----------|-----------------------------------------------------------------------------------------------|
| `src/app/page.tsx`        | `getServerSession`    | Session check for CTA swap only (no redirect) | WIRED  | Line 12: `getServerSession(authOptions)` called; line 13: `isSignedIn` computed from result; used in ternary at line 31 |
| `src/proxy.ts`            | `"/"`                 | PUBLIC_PATHS list (middleware bypass)        | WIRED    | `"/"` is in `PUBLIC_PATHS` array (line 5); `isPublicPath` returns true; middleware calls `NextResponse.next()` — no redirect |
| `src/components/PublicSiteShell.tsx` | `PublicNavClient` | `isSignedIn` prop              | WIRED    | Line 11: `isSignedIn` computed; line 20: passed as prop to `PublicNavClient`                  |

### Requirements Coverage

| Requirement | Source Plan | Description                                                               | Status    | Evidence                                                                                          |
|-------------|------------|---------------------------------------------------------------------------|-----------|---------------------------------------------------------------------------------------------------|
| UINV-01     | 05.2-01    | Authenticated users on public pages do not get redirected to /dashboard   | SATISFIED | `redirect("/dashboard")` removed from `page.tsx`; `/` is in PUBLIC_PATHS in proxy; no other public pages redirect |
| UINV-02     | 05.2-01    | Install and Home nav links function correctly for all auth states          | SATISFIED | `<a href="https://scanrook.sh">Install</a>` is external; `<Link href="/">Home</Link>` unguarded; no auth intercept |

**Note:** REQUIREMENTS.md does not exist as a standalone file in this project. UINV-01 and UINV-02 are defined only within ROADMAP.md (Phase 5.2 Requirements field). Both IDs claimed by 05.2-01-PLAN.md are accounted for and satisfied. No orphaned requirements found.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| None | -    | -       | -        | -      |

No TODO/FIXME comments, no placeholder returns, no stub handlers, no empty implementations found in the modified file.

### Human Verification Required

#### 1. Authenticated session CTA swap in browser

**Test:** Sign in to the application, then navigate to `/` in the browser.
**Expected:** The hero section shows a "Go to Dashboard" button linking to `/dashboard` — NOT "Sign in".
**Why human:** Server-side session state cannot be exercised programmatically without a live runtime and a valid session cookie.

#### 2. Authenticated user clicking Home from /docs

**Test:** While signed in, navigate to any `/docs` page, then click the "Home" link in the nav.
**Expected:** Browser stays on the `/` marketing page — no redirect to `/dashboard`.
**Why human:** Verifying the absence of a runtime redirect requires browser navigation with a live session.

#### 3. Install link opens scanrook.sh without auth intercept

**Test:** While signed in, click the "Install" nav link.
**Expected:** Browser navigates to `https://scanrook.sh` (external site) — no auth redirect, no Next.js route transition.
**Why human:** External navigation behavior requires a live browser.

### Gaps Summary

No gaps found. All six must-have truths are verified against the actual codebase:

- The `redirect("/dashboard")` block was removed from `src/app/page.tsx` in commit `f7b84b6`
- The `redirect` import from `next/navigation` was removed (zero occurrences remain in `page.tsx`)
- `isSignedIn` is correctly computed (`!!(session?.user?.id && !session.revoked)`) and used in a ternary CTA
- `/signin/page.tsx` retains its `redirect("/dashboard")` — intentionally preserved per user decision
- No other public pages (`/docs`, `/blog`, `/roadmap`) contain `redirect("/dashboard")` calls
- The middleware `proxy.ts` lists `"/"` in `PUBLIC_PATHS` — middleware will never redirect `/` regardless of auth state
- `PublicNavClient.tsx` "Install" is an external `<a>` tag to `https://scanrook.sh`; "Home" is an unguarded `<Link href="/">`
- `docs/layout.tsx` wraps `PublicSiteShell` with no auth redirect logic
- Build compiles cleanly: `✓ Compiled successfully` with no errors or warnings

Three items are flagged for human verification to confirm runtime behavior with a live session, but automated code analysis fully supports goal achievement.

---

_Verified: 2026-03-04T09:00:00Z_
_Verifier: Claude (gsd-verifier)_

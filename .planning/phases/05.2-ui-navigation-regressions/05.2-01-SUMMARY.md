---
phase: 05.2-ui-navigation-regressions
plan: 01
subsystem: ui
tags: [nextjs, react, auth, navigation, session]

# Dependency graph
requires:
  - phase: 02-public-site-shell
    provides: PublicSiteShell async server component with isSignedIn propagation
provides:
  - Home page without auth redirect, conditional CTA button based on session
  - Audit confirming only /signin has dashboard redirect
affects: []

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Remove unused redirect import when removing redirect calls"
    - "isSignedIn boolean computed from session?.user?.id && !session.revoked pattern"
    - "Conditional CTA ternary: isSignedIn ? Go to Dashboard link : Sign in link"

key-files:
  created: []
  modified:
    - scanrook-ui/src/app/page.tsx

key-decisions:
  - "Home page keeps getServerSession call for CTA swap but no longer uses redirect — authenticated users see full marketing page"
  - "Only /signin retains redirect('/dashboard') — intentional per prior user decision"

patterns-established:
  - "Pattern: Public pages check session for CTA context only, never to redirect — use PublicSiteShell isSignedIn for nav, getServerSession in page for hero CTA"

requirements-completed: [UINV-01, UINV-02]

# Metrics
duration: 2min
completed: 2026-03-04
---

# Phase 05.2 Plan 01: Home Page Auth Redirect Removal Summary

**Removed auth redirect from home page so authenticated users see full marketing content with a contextual "Go to Dashboard" CTA instead of being bounced to /dashboard**

## Performance

- **Duration:** 2 min
- **Started:** 2026-03-04T08:18:58Z
- **Completed:** 2026-03-04T08:20:10Z
- **Tasks:** 2
- **Files modified:** 1

## Accomplishments
- Removed `redirect("/dashboard")` from `src/app/page.tsx` — authenticated users now see the home page
- Removed unused `redirect` import from `next/navigation`
- Added `isSignedIn` boolean computed from session (same pattern as PublicSiteShell)
- CTA shows "Go to Dashboard" (linking /dashboard) when signed in; "Sign in" (linking /signin) when not
- Audited all app routes — only `/signin/page.tsx` retains redirect to /dashboard (intentional)
- Confirmed PublicNavClient "Install" link is external `<a href="https://scanrook.sh">` and "Home" is `<Link href="/">`
- Confirmed docs layout uses PublicSiteShell with no auth redirect logic
- Build passes cleanly

## Task Commits

Each task was committed atomically:

1. **Task 1: Remove home page redirect and add conditional CTA** - `f7b84b6` (feat)
2. **Task 2: Audit — no other public pages have unwanted auth redirects** - no code changes, audit-only

## Files Created/Modified
- `scanrook-ui/src/app/page.tsx` - Removed redirect block and import, added isSignedIn boolean, conditional CTA ternary

## Decisions Made
- Keep `getServerSession` call in page.tsx (needed for CTA swap even without redirect)
- Only `/signin/page.tsx` retains `redirect("/dashboard")` — intentional per existing user decision from Phase 02-02

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Home page nav regression is fixed; authenticated users can now browse the marketing site freely
- No blockers — next plan in phase 05.2 can proceed

---
*Phase: 05.2-ui-navigation-regressions*
*Completed: 2026-03-04*

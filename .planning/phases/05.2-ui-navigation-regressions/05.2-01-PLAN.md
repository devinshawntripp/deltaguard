---
phase: 05.2-ui-navigation-regressions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scanrook-ui/src/app/page.tsx
autonomous: true
requirements:
  - UINV-01
  - UINV-02
must_haves:
  truths:
    - "Authenticated user visiting / sees the marketing home page, not a redirect to /dashboard"
    - "Authenticated user sees 'Go to Dashboard' CTA instead of 'Sign in' on home page"
    - "Unauthenticated user sees 'Sign in' CTA on home page (unchanged behavior)"
    - "Home link from /docs navigates to / and shows the home page for all users"
    - "Install link remains external to https://scanrook.sh"
    - "/signin still redirects authenticated users to /dashboard"
  artifacts:
    - path: "scanrook-ui/src/app/page.tsx"
      provides: "Home page without auth redirect, conditional CTA"
      contains: "Go to Dashboard"
  key_links:
    - from: "scanrook-ui/src/app/page.tsx"
      to: "getServerSession"
      via: "session check for CTA swap only"
      pattern: "isSignedIn.*getServerSession"
---

<objective>
Fix public site navigation so authenticated users are not redirected away from the home page.

Purpose: Authenticated users clicking "Home" from /docs (or navigating to /) are currently redirected to /dashboard. This breaks public site navigation for signed-in users. The fix removes the redirect and swaps the CTA button text contextually.

Output: Updated home page that shows marketing content to all users with auth-aware CTA button.
</objective>

<execution_context>
@/Users/devintripp/.claude/get-shit-done/workflows/execute-plan.md
@/Users/devintripp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.2-ui-navigation-regressions/05.2-CONTEXT.md

@scanrook-ui/src/app/page.tsx
@scanrook-ui/src/components/PublicSiteShell.tsx
@scanrook-ui/src/components/PublicNavClient.tsx

<interfaces>
<!-- PublicSiteShell already computes isSignedIn and passes to PublicNavClient -->
<!-- The home page already imports getServerSession and authOptions -->

From scanrook-ui/src/components/PublicSiteShell.tsx:
```typescript
export default async function PublicSiteShell({ children }: { children: ReactNode })
// Computes: const isSignedIn = !!(session?.user?.id && !session?.revoked);
// Passes isSignedIn to PublicNavClient
```

From scanrook-ui/src/app/page.tsx (current):
```typescript
// Lines 13-16 — THE REDIRECT TO REMOVE:
const session = await getServerSession(authOptions);
if (session?.user?.id && !session.revoked) {
  redirect("/dashboard");
}
```

From scanrook-ui/src/app/(auth)/signin/page.tsx:
```typescript
// Lines 11-14 — KEEP THIS REDIRECT (per user decision):
const session = await getServerSession(authOptions);
if (session?.user?.id && !session.revoked) {
  redirect("/dashboard");
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove home page redirect and add conditional CTA</name>
  <files>scanrook-ui/src/app/page.tsx</files>
  <action>
In `src/app/page.tsx`:

1. Remove the redirect block (lines 13-16): Delete the `if (session?.user?.id && !session.revoked) { redirect("/dashboard"); }` block. Keep the `getServerSession` call — it is still needed for the CTA swap.

2. Compute `isSignedIn` from the session (same pattern as PublicSiteShell): `const isSignedIn = !!(session?.user?.id && !session.revoked);`

3. Remove the `redirect` import from `next/navigation` (no longer used in this file).

4. Replace the "Sign in" CTA link in the hero section (line 34):
   - Currently: `<Link href="/signin" className="btn-primary">Sign in</Link>`
   - When `isSignedIn` is true: `<Link href="/dashboard" className="btn-primary">Go to Dashboard</Link>`
   - When `isSignedIn` is false: `<Link href="/signin" className="btn-primary">Sign in</Link>`
   - Use a ternary inline: `isSignedIn ? <Link href="/dashboard" className="btn-primary">Go to Dashboard</Link> : <Link href="/signin" className="btn-primary">Sign in</Link>`

5. Do NOT touch `/signin/page.tsx` — its redirect to /dashboard for authenticated users is intentional per user decision.

6. Do NOT change PublicSiteShell, PublicNavClient, or any nav structure.
  </action>
  <verify>
    <automated>cd /Users/devintripp/Desktop/GitHub/scanrook/scanrook-ui && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>
  - The `redirect("/dashboard")` call is removed from page.tsx
  - The `redirect` import from `next/navigation` is removed from page.tsx
  - `isSignedIn` boolean is computed from session
  - CTA shows "Go to Dashboard" (linking to /dashboard) when signed in
  - CTA shows "Sign in" (linking to /signin) when not signed in
  - Build succeeds with no errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Audit and verify no other public pages have unwanted auth redirects</name>
  <files>scanrook-ui/src/app/page.tsx</files>
  <action>
Run a grep audit across all files in `scanrook-ui/src/app/` for `redirect` calls that send users to `/dashboard`. The audit should confirm:

1. `scanrook-ui/src/app/page.tsx` — NO redirect to /dashboard (fixed by Task 1)
2. `scanrook-ui/src/app/(auth)/signin/page.tsx` — HAS redirect to /dashboard (intentional, KEEP)
3. `scanrook-ui/src/app/docs/layout.tsx` — NO redirect (fully public, uses PublicSiteShell)
4. No other public pages (blog, roadmap, docs subpages) contain `redirect("/dashboard")`

The audit result should be documented in the plan summary. If any unexpected redirects are found in public pages, fix them using the same pattern as Task 1 (remove redirect, keep session for CTA swap if needed).

Also verify:
- PublicNavClient "Install" link is `<a href="https://scanrook.sh">` (external, not intercepted by auth)
- PublicNavClient "Home" link is `<Link href="/">` (will now work correctly for all users)
- DocsLayout uses PublicSiteShell with no auth redirect logic of its own
  </action>
  <verify>
    <automated>grep -rn 'redirect.*dashboard\|redirect.*signin' scanrook-ui/src/app/ --include="*.tsx" --include="*.ts" 2>/dev/null | grep -v node_modules | grep -v '.next'</automated>
  </verify>
  <done>
  - Only scanrook-ui/src/app/(auth)/signin/page.tsx contains redirect("/dashboard") — confirmed intentional
  - No public pages (docs, blog, roadmap, home) redirect authenticated users away
  - Install link confirmed as external href to scanrook.sh
  - Home link confirmed as Link to "/" with no redirect
  </done>
</task>

</tasks>

<verification>
1. `grep -rn 'redirect.*dashboard' scanrook-ui/src/app/ --include="*.tsx"` returns ONLY the /signin page
2. `grep 'Go to Dashboard' scanrook-ui/src/app/page.tsx` returns the conditional CTA
3. `grep 'isSignedIn' scanrook-ui/src/app/page.tsx` shows the boolean computation
4. `npx next build` succeeds without errors
</verification>

<success_criteria>
- Authenticated user visiting / sees the full marketing home page with "Go to Dashboard" CTA
- Unauthenticated user visiting / sees the full marketing home page with "Sign in" CTA
- Home link from /docs works for all users (no redirect)
- /signin still redirects authenticated users to /dashboard (unchanged)
- No other public pages redirect authenticated users to /dashboard
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/05.2-ui-navigation-regressions/05.2-01-SUMMARY.md`
</output>
